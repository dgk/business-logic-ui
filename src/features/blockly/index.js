/** @flow **/
import * as React from 'react'

type TProps = {
  props: {}
}

const Blockly = (props: TProps) => {
  console.log('\n\n\nprops', props)

  return <div>Blockly</div>
}

export default Blockly